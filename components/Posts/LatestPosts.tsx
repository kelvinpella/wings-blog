// import allPosts from "@/posts/posts.json";
// will be a list of all posts sorted in descending order. Only 9 posts will be shown as latest
// type AllPostType = typeof allPosts
export default function LatestPosts() {
//  const 
//   const posts= Object.keys(allPosts).map((postKey)=>allPosts[postKey])
  return <h1>Latest Posts</h1>;
}
